pipeline {
    agent any // Specifies the pipeline can run on any available agent
    stages {
        stage('Cleanup') {
            steps {
                cleanWs()
            }
        }

        stage('Clone Repository') {
            steps {
                checkout scm
            }
        }
 }
}
